name: $(TeamProject)_$(Date:yyyyMMdd)_$(Hours).$(Minutes).$(Seconds).$(Rev:r)

trigger:
- main

pool:
  name: pool

variables:
  ImageName: 'caluculator-image-$(Build.BuildId)'
  dockerRegistryServiceConnection: 'demoapp490970 service connection'
  dockerfilePath: './Dockerfile'
  tag: '$(Build.BuildId)'


stages:
- stage: Build
  displayName: Build image
  jobs:  
  - job: DockerImage
    displayName: Build and push Docker image
    steps:
     - task: Docker@2
       displayName: Build and publish image to Azure Container Registry
       inputs:
         repository: $(ImageName)
         command: buildAndPush
         Dockerfile: $(dockerfilePath)
         containerRegistry: $(dockerRegistryServiceConnection)
         tags: |
          $(tag)
    # - script: |
    #    docker images $(ImageName)
    #     docker tag $(ImageName):$(Build.BuildId) demoapp490970.azurecr.io/$(ImageName):$(Build.BuildId)
