trigger:
- main

pool:
  name: pool

variables:
  imageName: 'caluculator-image'
  version: $[format('{0:ddMMyyyy}', pipeline.startTime)]

name: $(TeamProject)_$(Date:yyyyMMdd)_$(Hours).$(Minutes).$(Seconds).$(Rev:r)

stages:
- stage: Build
  displayName: Build image
  jobs:  
  - job: DockerImage
    displayName: Build and push Docker image
    steps:
    - script: |
        echo "Building the Docker image with sudo..."
        echo "this is the docker $(ImageName):Latest"
        sudo docker build -t $(ImageName) .
      
